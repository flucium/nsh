mod ansi;
mod builtin;
mod manifest;
mod parser;
mod prompt;
mod shell;
mod variable;

fn main() {
    
    // let mut node = parser::Parser::new("ls | cat -b > output.txt | rev".chars().collect()).pop();

    

    
}
