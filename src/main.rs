// use nsh::*;

fn main() {
    // let tree = parser::Parser::new(parser::lexer::Lexer::new(
    //     "ping -c 10 flucium.commmmm > test.txt 2> err.txt;cat < err.txt;".chars().collect(),
    // ))
    // .parse()
    // .unwrap();

    // evaluator::Evaluator::new(parser::Node::Tree(tree))
    //     .eval()
    //     .unwrap();
}
